# Bolna integration

1. In your Bolna agent config, set the LLM provider to **Custom OpenAI-compatible**.
2. Use this base URL:
   `https://demo.masker.dev/agents/bolna-public-demo/s/<your-session>/v1`
3. Model name: any OpenAI-shaped model string (e.g. `gpt-4o-mini`).
4. Save and dial in.
